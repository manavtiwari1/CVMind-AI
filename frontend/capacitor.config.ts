import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.airesumechecker.app',
  appName: 'AI Resume Checker',
  webDir: 'dist'
};

export default config;
